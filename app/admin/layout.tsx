import { DashboardLayout } from "../../components/domains/admin/layout/dashboardlayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <DashboardLayout>{children}</DashboardLayout>
}
