import { DashboardLayout } from "../../components/domains/cliente/layout/dashboardlayoutc";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <DashboardLayout>{children}</DashboardLayout>
}
